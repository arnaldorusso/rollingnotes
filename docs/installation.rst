============
Installation
============

At the command line::

    $ easy_install rolling-notes

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv rolling-notes
    $ pip install rolling-notes